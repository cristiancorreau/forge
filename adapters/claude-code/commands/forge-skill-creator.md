# forge-skill-creator

Create a new SKILL.md from scratch and iterate with `forge eval` until it passes the quality gate.

Use the forge-skill-creator skill to start: $ARGUMENTS will be interpreted as the skill name and/or its purpose.

If $ARGUMENTS is empty, ask the user what the skill should do, when it triggers, and what it does NOT cover.
