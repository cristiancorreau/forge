# forge-skill-improver

Improve an existing SKILL.md with `forge eval` until it passes the quality gate, fixing the weakest category each round.

Use the forge-skill-improver skill on: $ARGUMENTS will be interpreted as the path to the SKILL.md to improve.

If $ARGUMENTS is empty, ask the user which skill file to improve.
